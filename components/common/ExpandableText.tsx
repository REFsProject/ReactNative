type ExpandableTextProps =
{
    text: string,
    isExpand: boolean,
    maxLenghtBeforeExpand: number,
    maxLenght: number,
    convertToScrollView?: boolean
}

export default function ExpandableText({text, state})
{
    if(state)
    {

    }
}